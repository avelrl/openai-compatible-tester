profiles:
  # Replace these placeholders with real model IDs for your target provider.
  - name: "default"
    chat_model: "your-chat-model"
    responses_model: "your-responses-model"
    # Keep the generic template conservative. Add model-specific tuning only
    # after the provider docs or a live reference run confirm it is supported.
    # chat_max_tokens_param: "max_completion_tokens"
    # reasoning_effort: "low"
    # temperature: 0
    extra: {}
